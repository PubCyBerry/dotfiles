# .wslconfig - WSL2 전역 설정
# 위치: C:\Users\<username>\.wslconfig
# 적용: wsl --shutdown 후 재시작 필요

[wsl2]
# 메모리 제한 (기본값: 전체 RAM의 50% 또는 8GB 중 작은 값)
memory=8GB

# CPU 코어 수 (기본값: 전체 논리 코어)
processors=4

# 스왑 크기 (기본값: RAM의 25%)
swap=2GB

# localhost 포워딩 활성화 (기본값: true)
localhostForwarding=true
